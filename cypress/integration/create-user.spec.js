context('create user page', () => {
	it('shows a two page tutorial when the page is loaded', () => {
		
	})

	it('will not allow the form to be submitted if the username is not unique', () => {

	})

	it('submits the correct data when the referral field is filled in', () => {
		
	})

	it('submits the correct data when the referral field is not filled in', () => {
		
	})
})